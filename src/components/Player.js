import React from "react" 

const Player = ({player}) => {
    // console.log(player)
    return (
        <div className="player-lobby">
         {player}
        </div> 
    )
}

export default Player;